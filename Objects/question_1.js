let person = {
  name:       'Bob',
  occupation: 'web developer',
  hobbies:    'painting',
};

// You can access the name of the person by calling person.name or person['name']
console.log(person.name);
console.log(person['name']);